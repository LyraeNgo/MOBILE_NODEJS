export default class SeatType {
  constructor({ seatTypeId, seatName, createdAt, updatedAt }) {
    this.seatTypeId = seatTypeId;
    this.seatName = seatName;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;
  }
}
